# UI 및 기본 설정
streamlit>=1.0,<2
python-dotenv>=1.0,<2
pydantic>=2.0,<3

# LangChain / LangGraph (호환성 확보를 위해 하한선만 유지)
langgraph>=0.2.0,<1.0.0
langchain-core>=0.3.0,<1.0.0
langchain-community>=0.3.0,<1.0.0
langchain-openai>=0.2.0,<1.0.0
langchain-text-splitters>=0.3.0,<1.0.0
langchain-huggingface>=0.0.3,<1.0.0

# LLM API 및 검색
openai>=1.0,<2
duckduckgo-search>=6.0
tenacity>=8.0,<9

# RAG / 벡터 검색 (버전 범위를 넓혀 충돌 방지)
ragas>=0.2.0,<1.0.0
faiss-cpu>=1.8,<2
sentence-transformers>=3.0,<4
scikit-learn>=1.3,<2
huggingface-hub>=0.20.0,<1.0.0
tiktoken>=0.7.0,<1.0.0
pypdf>=4.0,<5

# PDF 처리 및 OCR
pymupdf>=1.23,<2          # fitz — PDF 텍스트 추출 및 페이지 렌더링
rapidocr-onnxruntime>=1.3 # 스캔 PDF OCR

# 데이터베이스
psycopg2-binary>=2.9,<3   # Supabase PostgreSQL 직접 연결

# 데이터 처리 및 시각화
pandas>=2.0,<3
matplotlib>=3.7,<4
seaborn>=0.13,<1
scipy>=1.11,<2             # KDE 분포 시각화 (gaussian_kde)

# [핵심 수정] 버전 고정 해제 및 호환성 확보
# torch==2.2.0은 최신 서버 환경과 충돌하므로 최소 버전만 지정합니다.
torch>=2.2.0
# numpy 역시 특정 범위를 지정하기보다 최신 라이브러리들이 정하는 버전을 따르도록 합니다.
numpy